services:
  postgres:
    container_name: taskiq_django_db
    image: postgres:18
    environment:
      POSTGRES_DB: taskiq_django
      POSTGRES_USER: taskiq_django
      POSTGRES_PASSWORD: look_in_vault
    ports:
      - "5432:5432"
